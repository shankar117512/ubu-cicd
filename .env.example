cat > .env.example << 'EOF'
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
DB_NAME=myproject_db
DB_USER=myproject_user
DB_PASSWORD=yourpassword
DB_HOST=localhost
DB_PORT=5432
EOF

# Copy it as your actual .env
cp .env.example .env
